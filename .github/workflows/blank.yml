name: Roast My PR

on: 

jobs:
  roast:
    runs-on: ubuntu-lgithub-token` and `openai-api-key` inputs are passed correctly to theatest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Roast My PR
        uses: AlexanderOtavka/roast
        on:
  pull_request:
    # Run this action whenever a pull request is created or-my-pr@v1 updated
    types: [opened, synchronize]


    
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

          
 review:
    runs-on: ubuntu-latest
    steps:
    - uses```

Make sure that the `GITHUB_TOKEN`: actions/checkout@v2
    - name: Roast My PR
      secret is available in your repository settings. This token is automatically provided in GitHub Actions environments uses: AlexanderOtavka/roast-my-pr@v1.1.1
      with:
 and is used to authenticate API requests.

1        github-token: ${{ secrets.GITHUB_TOKEN }}
        openai-api-key: ${{ secrets.OPENAI_API_KEY }}
